# TODO

Meccha Chameleon 스타일 위장 게임 프로토타입의 다음 작업 후보다.

## 우선순위 기준

- `P0`: 다음 작업으로 바로 처리해야 플레이 가능한 루프가 생기거나 현재 구현 검증이 가능해지는 항목.
- `P1`: 핵심 재미와 확장성에 중요하지만 P0 검증 뒤에 붙이는 항목.
- `P2`: 품질, 운영, 제작 편의성을 높이는 항목.

## 작업 후보

| 우선순위 | 작업 | 이유 | 완료 기준 |
| --- | --- | --- | --- |
| P0 | `ChameleonPainter` 테스트 레벨 구성 | 플러그인 컴포넌트가 실제 에디터/레벨에서 생성, 표시, 색 변경되는지 확인해야 한다. | 테스트 맵에 `AChameleonHiderBodyActor`를 배치하고 PIE에서 몸체가 보이며 색상 변경이 동작한다. |
| P0 | 페인트용 머티리얼 제작 | 현재 C++은 `PaintColor` 계열 파라미터와 버텍스 컬러를 갱신하지만, 실제 시각화용 머티리얼 자산이 필요하다. | `PaintColor` 파라미터와 Vertex Color를 읽는 머티리얼을 만들어 몸체에 적용한다. |
| P0 | 플레이어 캐릭터에 hider body 연결 | 독립 액터만으로는 게임 루프 검증이 어렵다. 플레이어 Pawn/Character에 몸체와 paint component를 붙여야 한다. | 플레이어가 움직일 때 hider body가 따라가고 색상 위젯이 해당 paint component를 조작한다. |
| P0 | 색상 선택 UMG 표시/입력 연결 | 위젯 클래스는 구현됐지만 게임 내 표시, 입력 모드, 대상 컴포넌트 연결이 필요하다. | 키 입력으로 위젯을 열고 닫으며 스와치/슬라이더가 몸 색을 바꾼다. |
| P0 | 에디터 Live Coding 비활성 상태에서 일반 빌드 확인 | 현재 검증은 Live Coding 빌드 성공이다. 일반 UBT 빌드도 확인해야 안정적으로 커밋/패키징할 수 있다. | 에디터 종료 또는 Live Coding 비활성 후 `HideSeekEditor Win64 Development` 일반 빌드가 성공한다. |
| P1 | 브러시 칠하기 입력 구현 | 단순 전체 색 변경만으로는 원작식 위장 플레이가 약하다. 마우스/패드 조준으로 몸 일부를 칠해야 한다. | 화면 중앙 trace 또는 커서 trace로 몸체에 stroke를 찍고 반경/강도 조절이 가능하다. |
| P1 | 배경 색 샘플링 기능 | 위장 게임의 핵심은 주변 배경색을 골라 몸에 바르는 것이다. | 바라보는 표면이나 히트된 오브젝트에서 대표 색을 얻어 현재 paint color로 설정한다. |
| P1 | render target 기반 페인팅 검토 | 현재 stroke는 버텍스 컬러 갱신 방식이라 세밀한 지속 페인트에는 한계가 있다. | UV/RenderTarget 기반 페인트 백엔드 설계와 최소 구현을 추가한다. |
| P1 | 네트워크 복제 설계 | 멀티플레이 숨바꼭질이면 색상과 stroke 상태가 다른 클라이언트에 보여야 한다. | base color와 stroke 또는 paint texture 상태가 서버 권위로 복제된다. |
| P1 | 헌터 찾기/공격 판정 | 위장만으로는 게임이 성립하지 않는다. 헌터가 hider를 찾아 맞추는 룰이 필요하다. | 헌터 trace/발사 입력으로 hider를 적중시키고 라운드 상태에 반영한다. |
| P1 | 라운드 흐름 구현 | 숨는 시간, 찾는 시간, 승패 판정이 있어야 플레이 테스트가 가능하다. | 준비, 숨기, 탐색, 결과 상태가 GameMode/GameState에서 전환된다. |
| P2 | 몸체 메시 최적화/LOD | 현재 절차 메시 해상도는 프로토타입용이다. 다수 플레이어에서는 비용을 줄여야 한다. | 해상도 preset, 캐싱, LOD 또는 static mesh bake 전략을 정한다. |
| P2 | 캐릭터 애니메이션/리깅 방향 결정 | 절차 메시는 즉시 보이지만 제대로 걷는 캐릭터에는 skeletal mesh 또는 단순 애니메이션 전략이 필요하다. | capsule/mesh 상대 위치, 걷기 애니메이션 또는 squash/stretch 방식 중 하나를 선택한다. |
| P2 | 색상 UI UX 정리 | 현재 위젯은 기능 중심이다. 실제 게임에서는 빠르게 색을 고르고 비교할 수 있어야 한다. | 최근 색, 환경 팔레트, 선택 미리보기, 취소/확정 흐름을 정리한다. |
| P2 | 치트/자동 페인트 대응 방향 검토 | 자동 배경 매칭은 게임성을 망칠 수 있다. 의도한 난이도를 유지할 제한이 필요하다. | 샘플링 쿨다운, 색상 양자화, 브러시 비용 등 제한안을 정한다. |
| P2 | 플러그인 모듈 분리 검토 | 현재는 단일 Runtime 모듈이다. UI 의존을 더 낮추려면 Runtime/UMG 모듈 분리를 고려할 수 있다. | 실제 재사용 요구가 생기면 `ChameleonPainterRuntime`과 `ChameleonPainterUMG` 분리 여부를 결정한다. |

## 추천 진행 순서

1. 테스트 레벨과 페인트 머티리얼을 만든다.
2. 플레이어 캐릭터에 hider body와 color picker를 연결한다.
3. 브러시 입력과 배경 색 샘플링을 붙인다.
4. 헌터 공격 판정과 라운드 흐름을 구현한다.
5. 멀티플레이 복제와 최적화를 검토한다.
